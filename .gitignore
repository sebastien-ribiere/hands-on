# Golden Thread source published locally for the demo. Rebuilt by
# demo/publish-source.sh from the tracked golden-thread-source/ tree.
.demo/

__pycache__/
*.pyc
.pytest_cache/

# The CI artifact, copied out of the job by demo/run-ci-locally.sh so it can be
# read after the run. GitLab keeps its own copy; this one is a convenience.
golden-thread-report.json
